<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfFaq"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity" xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions" x:Class="WpfFaq.MainWindow"
        mc:Ignorable="d"
    x:Name="FaqWindow"
        Title="Faq" Height="350" Width="800" Loaded="Window_Loaded" MinWidth="664" Icon="monkey.ico" >

    <Window.Resources>
        <Style x:Key="TextBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Height" Value="22"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="Margin" Value="4"/>
        </Style>

        <Style x:Key="ToogleButtonStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Width" Value="22"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Width" Value="22"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <DataTemplate x:Key="FaqItemTemplate">
            <StackPanel Width="500" Margin="20">
                <TextBox Margin="0,0,0,10" ScrollViewer.PanningMode="VerticalOnly" BorderBrush="{x:Null}" BorderThickness="0" Background="Transparent" IsReadOnly="True" Text="{Binding Question, Mode=OneWay}" TextWrapping="Wrap" FontWeight="Bold" />
                <TextBox BorderBrush="{x:Null}" ScrollViewer.PanningMode="VerticalOnly" BorderThickness="0" Background="Transparent" IsReadOnly="True" Text="{Binding Answer, Mode=OneWay}" TextWrapping="Wrap" />
            </StackPanel>
        </DataTemplate>
        <ItemsPanelTemplate x:Key="ItemsPanelTemplate1">
            <VirtualizingStackPanel/>
        </ItemsPanelTemplate>

    </Window.Resources>
    <d:DataContext>
        <Binding Source="{StaticResource SampleDataSource}"/>
    </d:DataContext>
    <Grid x:Name="grid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="28"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="SearchVisualStateGroup">
                <VisualStateGroup.Transitions>
                    <VisualTransition GeneratedDuration="0:0:0.2">
                        <VisualTransition.GeneratedEasingFunction>
                            <ExponentialEase EasingMode="EaseOut"/>
                        </VisualTransition.GeneratedEasingFunction>
                    </VisualTransition>
                </VisualStateGroup.Transitions>
                <VisualState x:Name="Normal"/>
                <VisualState x:Name="Search">
                    <Storyboard>
                        <BooleanAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.IsEnabled)" Storyboard.TargetName="canvas">
                            <DiscreteBooleanKeyFrame KeyTime="0" Value="True"/>
                        </BooleanAnimationUsingKeyFrames>
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(FrameworkElement.Width)" Storyboard.TargetName="canvas">
                            <EasingDoubleKeyFrame KeyTime="0" Value="211"/>
                        </DoubleAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
            <VisualStateGroup x:Name="ToolBoxSearchStateGroup">
                <VisualStateGroup.Transitions>
                    <VisualTransition GeneratedDuration="0:0:0.1"/>
                </VisualStateGroup.Transitions>
                <VisualState x:Name="Default"/>
                <VisualState x:Name="Hover">
                    <Storyboard>
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="toogleSearchBotton">
                            <EasingDoubleKeyFrame KeyTime="0" Value="1"/>
                        </DoubleAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
            <VisualStateGroup x:Name="NewItemStateGroup">
                <VisualStateGroup.Transitions>
                    <VisualTransition GeneratedDuration="0:0:0.2"/>
                </VisualStateGroup.Transitions>
                <VisualState x:Name="View"/>
                <VisualState x:Name="AddItem">
                    <Storyboard>
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(FrameworkElement.Width)" Storyboard.TargetName="canvasAddNewItem">
                            <EasingDoubleKeyFrame KeyTime="0" Value="461"/>
                        </DoubleAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
            <VisualStateGroup x:Name="ToolBoxNewItemStateGroup">
                <VisualState x:Name="NewItemDefault"/>
                <VisualState x:Name="NewItemHower">
                    <Storyboard>
                        <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(TextElement.Foreground).(SolidColorBrush.Color)" Storyboard.TargetName="AddNewItem">
                            <EasingColorKeyFrame KeyTime="0" Value="#FF072C15"/>
                        </ColorAnimationUsingKeyFrames>
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="AddNewItem">
                            <EasingDoubleKeyFrame KeyTime="0" Value="1"/>
                        </DoubleAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Canvas x:Name="ToolBarCanvas" Grid.Row="2" >
            <ToggleButton x:Name="toogleSearchBotton" Style="{DynamicResource ToogleButtonStyle}" Opacity="0.2" >
                <ToggleButton.ContentTemplate>
                    <DataTemplate>
                        <Canvas>
                            <Ellipse Height="12.6" Canvas.Left="-2.6" Canvas.Top="-10" Stroke="Black" StrokeThickness="2" Width="12.6">
                                <Ellipse.Fill>
                                    <RadialGradientBrush>
                                        <GradientStop Color="#FF495889" Offset="0"/>
                                        <GradientStop Color="#FFD2D8EB" Offset="1"/>
                                    </RadialGradientBrush>
                                </Ellipse.Fill>
                            </Ellipse>
                            <Path Data="M10,0 L0,10" Fill="#FFF4F4F5" Height="10" Stroke="Black" StrokeThickness="2" Canvas.Top="0" Canvas.Left="-10" Width="10"/>
                        </Canvas>
                    </DataTemplate>
                </ToggleButton.ContentTemplate>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Checked">
                        <ei:GoToStateAction StateName="Search"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="Unchecked">
                        <ei:GoToStateAction StateName="Normal"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseEnter">
                        <ei:GoToStateAction StateName="Hover"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseLeave">
                        <ei:GoToStateAction StateName="Default"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </ToggleButton>
        </Canvas>

        <Canvas Grid.Row="1" Height="30">
            <ToggleButton Style="{DynamicResource ToogleButtonStyle}" Content="+" x:Name="AddNewItem" Opacity="0.2">
                <ToggleButton.Template>
                    <ControlTemplate>
                        <Canvas>
                            <TextBlock Canvas.Left="4" Canvas.Top="-4" FontSize="20" Foreground="DarkGreen" FontWeight="Bold">+</TextBlock>
                        </Canvas>
                    </ControlTemplate>
                </ToggleButton.Template>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Checked">
                        <ei:GoToStateAction StateName="AddItem"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="Unchecked">
                        <ei:GoToStateAction StateName="View"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseEnter">
                        <ei:GoToStateAction StateName="NewItemHower"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseLeave">
                        <ei:GoToStateAction StateName="NewItemDefault"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </ToggleButton>
        </Canvas>

        <Canvas x:Name="canvasAddNewItem" Width="0" ClipToBounds="True" HorizontalAlignment="Left" Grid.Row="1" Grid.Column="1" IsEnabled="{Binding IsChecked, ElementName=AddNewItem}">
            <TextBox x:Name="questionText" Style="{StaticResource TextBoxStyle}"></TextBox>
            <TextBox x:Name="answerText" Canvas.Left="210" Style="{StaticResource TextBoxStyle}"></TextBox>
            <Button Canvas.Left="420" Style="{StaticResource ButtonStyle}" Click="Button_Click">OK</Button>
        </Canvas>

        <Canvas FocusManager.FocusedElement="{Binding ElementName=searchText}" x:Name="canvas" Grid.Row="2" Grid.Column="1" Width="0" HorizontalAlignment="Left" ClipToBounds="True" IsEnabled="False" Height="30" >
            <TextBox x:Name="searchText"  Style="{StaticResource TextBoxStyle}" Canvas.Left="2.2" Margin="0" Canvas.Top="4" >
            </TextBox>
        </Canvas>

        <ScrollViewer Grid.Column="1" Padding="0,20" PanningMode="VerticalOnly" ManipulationBoundaryFeedback="ScrollViewer_ManipulationBoundaryFeedback" Grid.ColumnSpan="2" >
            <ItemsControl x:Name="listBox" ItemTemplate="{DynamicResource FaqItemTemplate}" ItemsSource="{Binding}" Margin="40,0" ItemsPanel="{DynamicResource ItemsPanelTemplate1}" />
        </ScrollViewer>
    </Grid>
</Window>
